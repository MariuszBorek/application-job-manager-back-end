package com.manager.repository;

import com.manager.model.Scupper;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScupperRepository extends JpaRepository<Scupper, Integer> {
}
